# .github/workflows/ossguard.yml
# Add this to your repository to enable OSSGuard security reviews on PRs

name: OSSGuard

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  statuses: write

jobs:
  security-review:
    name: Security Review
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: kirankotari/ossguard-app@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
